import 'package:flutter/material.dart';

class CreateSurveyRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        title: new Text(
          'Create Your Survey',
        ),
        centerTitle: true,
      ),
    );
  }
}
